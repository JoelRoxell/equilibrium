/* Pixel to rem */
rem(val)
  u = unit(val)

  if (u is 'px')
    return  unit(val / remBase, 'rem')
  else
    return unit(val / remBase, 'rem')

/* Screen */
lesserThan(device)
  condition = '(max-width: %s)' % device
  @media condition
    { block }
